# Shadcn Vue 3D 项目模板

一个基于 Vue 3 + Shadcn Vue + Three.js 的 3D 项目模板，专为国内开发者优化。

## ✨ 特性

- 🎨 **Shadcn Vue** - 现代化、可定制的 UI 组件
- 🌙 **深色主题** - 专为 3D 项目优化的配色
- 📦 **Three.js** - 强大的 3D 渲染引擎
- 🚀 **Vite** - 极速的开发体验
- 💪 **TypeScript** - 完整的类型支持
- 🎯 **零外部依赖** - 部署后无需加载任何 CDN

## 🚀 快速开始

### 1. 安装依赖

```bash
# 推荐使用国内镜像
npm config set registry https://registry.npmmirror.com

# 安装依赖
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

### 3. 构建生产版本

```bash
npm run build
```

## 📁 项目结构

```
src/
├── assets/           # 静态资源和全局样式
│   └── index.css     # Tailwind 配置和主题变量
├── components/       # 组件
│   ├── ui/           # Shadcn UI 基础组件
│   │   ├── Button.vue
│   │   ├── Card.vue
│   │   ├── Input.vue
│   │   └── Slider.vue
│   ├── Sidebar.vue   # 侧边栏
│   ├── ThreeScene.vue # 3D 场景
│   └── ControlPanel.vue # 控制面板
├── views/            # 页面视图
│   ├── HomeView.vue
│   ├── ModelsView.vue
│   └── SettingsView.vue
├── router/           # 路由配置
├── stores/           # Pinia 状态管理
├── lib/              # 工具函数
│   └── utils.ts
├── App.vue           # 根组件
└── main.ts           # 入口文件
```

## 🎨 自定义主题

编辑 `src/assets/index.css` 中的 CSS 变量：

```css
.dark {
  --background: 224 71% 4%;      /* 背景色 */
  --foreground: 213 31% 91%;    /* 前景色 */
  --primary: 239 84% 67%;       /* 主色调 */
  /* ... */
}
```

## 🧩 添加新组件

Shadcn Vue 的组件在 `src/components/ui/` 目录下，你可以：

1. 直接修改现有组件
2. 复制组件并自定义
3. 从 [shadcn-vue](https://www.shadcn-vue.com) 官网添加更多组件

## 📦 部署

构建后的文件在 `dist/` 目录：

```bash
npm run build
```

可以部署到任何静态托管服务：
- Vercel
- Netlify  
- 阿里云 OSS
- 腾讯云 COS
- Nginx

**无需担心 CDN 访问问题，所有资源都已本地化！**

## 🔧 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.4+ | 渐进式框架 |
| Vite | 5.0+ | 构建工具 |
| TypeScript | 5.3+ | 类型系统 |
| Tailwind CSS | 3.4+ | 原子化 CSS |
| Three.js | 0.160+ | 3D 引擎 |
| Pinia | 2.1+ | 状态管理 |
| Vue Router | 4.2+ | 路由 |

## 📄 License

MIT
